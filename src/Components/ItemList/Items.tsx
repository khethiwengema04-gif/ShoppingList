import React from 'react'


const Items = () => {
    return (
        <div>
            {/* {
                Items.ItemList.map((link) => (
                    <Card key={link.id} ItemList={link}
                        onView={() => ({})}
                        onDelete={() => ({})} />
                ))
            } */}
        </div>
    )
}

export default Items